-- Active: 1786433062240@@127.0.0.1@5432@yipethome
DROP TABLE IF EXISTS rooms;
CREATE TABLE rooms (
  id INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  name VARCHAR(100) NOT NULL,
  type INTEGER NOT NULL,
  shop_id INTEGER NOT NULL,
  PRIMARY KEY (id)
) ;

ALTER TABLE rooms
ADD CONSTRAINT fk_rooms_shop
FOREIGN KEY (shop_id) REFERENCES shops(id);

INSERT INTO rooms OVERRIDING SYSTEM VALUE VALUES (DEFAULT, 'Grooming Room', 0, 1);
INSERT INTO rooms OVERRIDING SYSTEM VALUE VALUES (DEFAULT, 'Washing Room', 1, 1);